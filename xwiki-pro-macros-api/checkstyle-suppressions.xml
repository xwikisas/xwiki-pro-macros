<?xml version="1.0"?>

<!DOCTYPE suppressions PUBLIC
        "-//Puppy Crawl//DTD Suppressions 1.0//EN"
        "http://www.puppycrawl.com/dtds/suppressions_1_0.dtd">

<suppressions>
  <!-- ClassFanOutComplexity is mostly triggered because manipulating both XDOM and macros produces a lot of imports.
   Splitting the code into several files to address the warning probably complexifies it, which would be
  counter-productive -->
  <suppress checks="ClassFanOutComplexity"
            files="src/main/java/com/xwiki/macros/excerptinclude/internal/macro/ExcerptIncludeMacro\.java"/>

  <!-- ClassFanOutComplexity is mostly triggered because manipulating both XDOM and macros produces a lot of imports.
   Splitting the code into several files to address the warning probably complexifies it, which would be
  counter-productive -->
  <suppress checks="ClassFanOutComplexity"
            files="src/main/java/com/xwiki/macros/viewfile/internal/macro/async/ViewFileAsyncFullRenderer\.java"/>
</suppressions>
